/*
 * BarGraph Messages
 *
 * This contains all the text for the BarGraph component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BarGraph';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BarGraph component!',
  },
});
