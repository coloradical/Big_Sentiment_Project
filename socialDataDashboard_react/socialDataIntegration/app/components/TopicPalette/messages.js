/*
 * TopicPalette Messages
 *
 * This contains all the text for the TopicPalette component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TopicPalette';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopicPalette component!',
  },
});
